using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Microsoft.Xna.Framework;
using Microsoft.Xna.Framework.Graphics;

namespace Asteroid_Belt_Assault
{
    class PowerUPManger
    {
        /* private Texture2D texture;
         private Rectangle initialFrame;
         public List<Power> Speedy = new List<Power>();
         public List<Power> Shield = new List<Power>();
         public List<Power> Shooty = new List<Power>();
         protected Vector2 location = Vector2.Zero;
         protected Vector2 velocity = Vector2.Zero;
         public ShotManager PowShotManager;

             public PowerUPManger(Vector2 location, Texture2D texture, Rectangle initialFrame, Vector2 velocity)
             {
                 this.texture = texture;
                 this.initialFrame = initialFrame;
                 this.location = location;
                 this.velocity = velocity;

                 PowShotManager = new ShotManager(texture, new Rectangle(0, 300, 5, 5), 4, 2, 150f, screenBounds);
             }

             public void SpawnSpeedy()
             {
                 Power Fast = new Power(texture, new Vector2(40, 50), initialFrame, frameCount);
                 Speedy.Add(Fast);
             }
             public void SpawnShield()
             {
                 Power Protect = new Power(texture, new Vector2(40, 50), initialFrame, frameCount); 
             }
         */

    }
}
